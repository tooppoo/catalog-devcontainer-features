{
    "id": "reportage",
    "version": "0.1.0",
    "name": "reportage",
    "description": "Installs reportage, an explicit, runtime-agnostic, coverage-aware E2E scenario runner with shell-like actions.",
    "documentationURL": "https://github.com/tooppoo/catalog-devcontainer-features/tree/main/src/reportage",
    "licenseURL": "https://github.com/tooppoo/catalog-devcontainer-features/blob/main/LICENSE",
    "options": {
        "version": {
            "type": "string",
            "description": "reportage release tag to install. Use latest to install the latest release.",
            "default": "latest"
        },
        "install_dir": {
            "type": "string",
            "description": "Directory where reportage will be installed.",
            "default": "/usr/local/bin"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
